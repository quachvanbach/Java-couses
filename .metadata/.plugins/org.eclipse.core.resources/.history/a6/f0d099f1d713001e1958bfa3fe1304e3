import java.util.Arrays;

public class BangCuuChuong {
	public static void main(String[] args) {
		Array arr = new Arrays.copyOf(1, 2, 3);
		for (int x : arr) {
			
		}
		arry.forEach(item -> {
			
		})
		for (int i = 2; i <= 9; i++) {
			for (int j = 1; j <= 10; i++) {
				System.out.println(i + " * " + j + " = ");
				list.stream().map(item -> Ơ
						})
			}
		}
	}
}
